from flask_api import db # the app running this module

#class users(db.Model):
#    username = db.Column(db.String(20), unique=True, nullable=False)
#
#    def __repr__(self):
#        return f"users('{self.username})"